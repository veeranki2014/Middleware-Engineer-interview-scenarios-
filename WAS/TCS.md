Tell about your self.
Websphere build 1-5(build & support)
Difference between webSphere vs WebLogic Vs JBoss? (Security transactions 2-way authentication )
What is use of web server? If no web content why do we required webserver?
Five 5 applications are there, server3(one JVM) is not working, error is HA Manager issue?
Auto restart of websphere in os? /etc/init.d
WAS kernel?
Difference between refresh and fix pack?
When do we required manual synchranization required? DB changes or configuration?
GC Policy and fine tuning the JVM parameters like younger & Older generation parameters ? ( permgen)
Load balancer, webserver and application server, permission denied error, whenever server reboot happening permission denied error in websphere? (Os level issue)
TLS version is changes on browser, how to upgrade in websphere level?
How to check the which TLS version on browser?
Want to delete the cert from keystone, what command do we need to use? Keytool -remove kdb
Migrate application for v8.5.5 to v9.0, what are the steps do we need to take?
Backupconfig in upgradation? Migration?
SSO configuration on websphere? Siteminder?SAML?
Inode?
The inode is a unique identifier in the file system. It is used by the operating system to locate and manage files and directories. When you create a new file or directory, the file system assigns it a unique inode number.
Ls -l <filename>
How to check how many requests to the application servers or webserver?
Ans) https://community.ibm.com/community/user/wasdevops/viewdocument/tips-for-using-the-ihs-access-log-f?CommunityKey=5c4ba155-561a-4794-9883-bb0c6164e14e&tab=librarydocuments